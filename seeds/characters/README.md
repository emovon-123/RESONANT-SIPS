# Characters Seed Directory

This directory centralizes role management:

- presets/: built-in preset full character profiles (JSON)
- added/: user-added full character profiles cached locally (JSON)

Both folders use the same JSON profile schema.

Current preset source consumed by frontend:
- seeds/characters/presets/5738g.json
