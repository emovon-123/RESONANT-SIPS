# 对话完成项汇总（2026-04-16）

本文档汇总本次对话中已经完成的工作、回退记录和当前生效状态。

## 1. API 可用性验证

### 1.1 DeepSeek Key 验证
- 已按用户要求尝试直连验证。
- 由于用户中途取消，未形成最终可用性结论。

### 1.2 Gemini Key 验证（Google 原生接口）
- 已执行最小请求验证。
- 返回结果：`API_KEY_INVALID`（无效 key）。
- 结论：当时本地读取到的 Gemini key 不可用。

### 1.3 302.AI OpenAI 兼容接口验证
- 对旧 key 测试结果：`401`（不可用）。
- 对新 key 测试结果：
  - `/v1/models` 返回成功。
  - `/v1/chat/completions` 返回成功并有 `choices`。
- 结论：新 key 当时可用。

## 2. localApiKeys 配置改动与回退

### 2.1 曾完成的改动（后续已回退）
- 曾将可用 key 写入配置，并切过 302.AI 相关 endpoint。
- 曾切到 Gemini provider 并验证连通性。

### 2.2 最终回退到初始态（已完成）
- 用户要求“回到最初，不要加 apikey”。
- 已完成回退：
  - `src/config/localApiKeys.js` 恢复为占位 key（`your api key`）。
  - `provider` 恢复为空字符串。
  - 移除新增脚本命令与脚本文件。

## 3. 角色 YAML -> Gemini 情绪验证脚本

### 3.1 曾新增（后续已删除）
- 曾新增 `scripts/validate-character-emotion.mjs`。
- 曾新增 `npm run validate:emotion -- <script-name>` 命令。
- 功能：按脚本名读取 `polyu-storyworld/characters` 下 YAML，调用 Gemini 输出情绪 JSON。

### 3.2 当前状态
- 用户要求回退到最初后，以上新增内容已删除，不在当前代码中。

## 4. 货币与商店系统评估结论

- 已完成代码核查，结论为：
  - 货币系统有实装（有收入、扣费、持久化）。
  - 商店系统有实装（购买后影响解锁并影响可选内容）。

## 5. 按用户要求调整：保留金币、取消购买解锁

- 已完成：将解锁数据改为“全量提前解锁”。
- 已完成：旧存档读取时自动补全为全解锁。
- 已保留：金币系统（收入结算逻辑）仍然生效。

涉及文件：
- `src/utils/storage.js`

## 6. 按用户要求调整：暂时移除商店

- 已完成：从主流程下线商店入口与商店面板（不删除底层相关代码）。
- 已完成：日结算弹窗移除商店区块，仅保留结算信息与“开始新的一天”。

涉及文件：
- `src/App.jsx`
- `src/pages/GamePage.jsx`
- `src/components/Game/DayEndModal.jsx`

## 7. 修复“新游戏直接进入调酒”问题

### 7.1 发现根因
- 根因是旧会话键（`bartender_game_session`）在切槽位时被残留复用。
- 会话恢复将 `guessedCorrectly=true` 恢复后，界面直接进入调酒阶段。

### 7.2 已完成修复
- 在 `hydrateLegacyStorageFromGameState` 中，先清理 legacy 键，再按当前槽位快照回填。
- 目的：避免新槽位被上一个槽位的本地会话状态污染。

涉及文件：
- `src/utils/saveRepository.js`

## 8. 安全与凭证说明

- 对话中出现过明文 key。
- 建议：将历史泄露 key 作废并轮换，仅保留有效新 key，后续优先使用环境变量注入。

## 9. 当前代码状态（总结）

当前生效的关键状态：
- API 配置已回到“未预置真实 key”的初始方式。
- 金币系统保留。
- 购买解锁门槛已取消（全量提前解锁）。
- 商店入口/商店面板已从主流程下线。
- “新游戏直接进调酒”的会话污染问题已修复。

## 10. 按用户要求调整：取消章节系统，仅保留天数推进

### 10.1 已完成内容
- 已完成：章节推进逻辑停用（`processDayEnd` 不再推进章节）。
- 已完成：章节 UI 标识隐藏（头部不再显示第 X 章）。
- 已完成：顶部“店铺阶段/酒吧等级（如新开的店等阶段提示）”显示关闭。
- 已完成：结局触发去除“必须第 5 章”前置门槛，避免章节停用后结局分支失效。

涉及文件：
- `src/hooks/useChapterSystem.js`
- `src/pages/GamePage.jsx`

## 11. 按用户要求调整：每天固定 3 位顾客

### 11.1 已完成内容
- 已完成：每日顾客上限由 5 调整为 3。
- 已完成：日结束判定修正为“已服务到当日最后一位且无下一位顾客”，避免队列预填充导致提前日结。

涉及文件：
- `src/hooks/useCustomerFlow.js`
- `src/hooks/gameHandlers/useCustomerDayHandlers.js`

## 12. 按用户要求调整：暂时移除奖励事件功能

### 12.1 已完成内容
- 已完成：事件系统增加总开关并置为关闭。
- 已完成：随机事件、奖励事件、事件链触发全部短路为 no-op。
- 已完成：对话阶段与递酒后的事件触发点停用。
- 已完成：事件弹窗不再渲染。

涉及文件：
- `src/hooks/useBarEvents.js`
- `src/hooks/gameHandlers/useServeProgressHandlers.js`
- `src/hooks/gameHandlers/useDialogueHandlers.js`
- `src/pages/GamePage.jsx`

## 13. 按用户要求调整：暂时无主线，聚焦接待顾客与情绪分析

### 13.1 已完成内容
- 已完成：主线叙事总开关增加并置为关闭。
- 已完成：日结不再触发章节转场、回忆碎片、结局流程。
- 已完成：章节转场 / 回忆碎片 / 结局 Overlay 全部受开关保护，不再显示。

涉及文件：
- `src/hooks/useChapterSystem.js`
- `src/pages/GamePage.jsx`

## 14. 按用户要求调整：暂时移除图鉴与成就系统

### 14.1 已完成内容
- 已完成：成就系统增加总开关并置为关闭，导出 API 保持兼容但逻辑为 no-op。
- 已完成：成就通知弹窗渲染停用。
- 已完成：首页图鉴入口隐藏，未读成就角标隐藏。
- 已完成：`encyclopedia` 路由禁用（请求进入时回落首页）。
- 已完成：黄金组合“发现入图鉴”写入逻辑停用。

涉及文件：
- `src/hooks/useAchievements.js`
- `src/pages/GamePage.jsx`
- `src/pages/HomePage.jsx`
- `src/App.jsx`
- `src/hooks/gameHandlers/useServeProgressHandlers.js`

## 15. 按用户要求调整：移除“今晚影响”，优先核心循环

### 15.1 已完成内容
- 已完成：氛围开场页“今晚影响”区块不再展示。
- 已完成：氛围 modifiers 在运行时归零（如 `priceMultiplier=1`、`targetShift=0`、`trustBonus=0`），确保不影响价格/目标/信任等核心结算。
- 已保留：天气、音乐、文案等氛围叙事展示（仅视觉/叙事层）。

涉及文件：
- `src/components/Atmosphere/AtmosphereOverlay.jsx`
- `src/hooks/useBarAtmosphere.js`

## 16. 本轮稳定性验证

- 已对上述调整多次执行 `npm run build`，结果均成功。
- 构建过程仍存在既有 Vite 提示（`aiCustomers.js` 动态与静态同时引用），不属于本轮改动引入的错误。

## 17. 当前生效状态（更新版）

当前实际生效的运行策略为：
- 以“接待顾客 + 情绪分析 + 调酒 + 日结”作为核心闭环。
- 无章节推进。
- 无顶部店铺阶段/酒吧等级阶段提示（避免章节感/主线感）。
- 无主线叙事触发（转场/碎片/结局均停用）。
- 无奖励事件/随机事件/事件链。
- 无图鉴入口与图鉴写入。
- 无成就判定与成就通知。
- 无“今晚影响”对数值结算的修正。
- 每天固定 3 位顾客。

## 18. 本轮新增：添加角色功能与入口调整

### 18.1 已完成内容
- 已完成：新增“自定义角色池”能力，支持添加多个角色 ID（如 `5738g`）。
- 已完成：支持对已添加角色进行“允许出现”勾选与取消勾选（`activeCharacterIds`）。
- 已完成：支持移除已添加角色。
- 已完成：角色 ID 进行格式校验（仅允许字母、数字、下划线、短横线）。
- 已完成：角色池配置写入本地存储并纳入槽位存档同步快照。

涉及文件：
- `src/utils/storage.js`
- `src/utils/saveRepository.js`
- `server/save-server.mjs`

### 18.2 顾客生成链路接入（已完成）
- 已完成：新增按角色 ID 生成顾客配置的生成函数。
- 已完成：当日顾客生成改为“优先使用已勾选角色，数量不足时随机补足”。
- 已完成：首位顾客预加载、游戏初始化首位顾客、日内补位、新一天队列构建均已接入上述规则。

涉及文件：
- `src/utils/ai/customerGeneration.js`
- `src/utils/aiService.js`
- `src/App.jsx`
- `src/hooks/useGameInit.js`
- `src/hooks/useCustomerFlow.js`
- `src/hooks/gameHandlers/useCustomerDayHandlers.js`

### 18.3 入口调整（已完成）
- 已完成：角色管理入口从首页移出，不再在主菜单直接配置。
- 已完成：点击“新的游戏”后先进入“新游戏配置页”，再由用户确认开始新游戏。
- 已完成：存档页“创建并开始”同样走“新游戏配置页”流程。

涉及文件：
- `src/App.jsx`
- `src/pages/HomePage.jsx`
- `src/pages/NewGameSetupPage.jsx`
- `src/pages/NewGameSetupPage.css`

### 18.4 验证
- 已完成：本轮改动后执行 `npm run build`，构建通过。
- 说明：仍存在既有 Vite 提示（`aiCustomers.js` 动态与静态同时引用），非本轮新增错误。

## 19. 当前生效状态（补充）

在第 17 节基础上，额外生效：
- “新的游戏”进入后先到“新游戏配置页”。
- 可在“新游戏配置页”管理自定义角色池（添加/勾选/移除）。
- 顾客生成目前已切换为“仅自定义角色模式”：未启用角色时不再随机补位，直接提示并阻止开局。

## 20. 本轮新增：Storyworld YAML 读取与 MCP 风格接口落地

### 20.1 已完成内容
- 已完成：新增服务端 `storyworld-service`，支持本地目录优先检索 YAML，未命中时远端 GitHub 回退读取。
- 已完成：新增 MCP 风格接口：`/api/mcp/character/get_by_name` 与 `/api/mcp/character/search`。
- 已完成：前端新增 `storyworldRepository` 调用层，并将角色顾客生成接入真实 YAML 字段映射（name/personality/backstory/dialogueStyle 等）。
- 已完成：Vite 代理新增 `/api/mcp`，并增加 `yaml` 依赖用于服务端解析。

涉及文件：
- `server/storyworld-service.mjs`
- `server/save-server.mjs`
- `src/utils/storyworldRepository.js`
- `src/utils/ai/customerGeneration.js`
- `vite.config.js`
- `package.json`

## 21. 本轮新增：角色生成切换为“仅自定义角色模式”

### 21.1 已完成内容
- 已完成：移除“无 active 角色时随机默认角色补位”路径。
- 已完成：`generateCustomerWithCharacterPool` 在无启用角色时抛出 `no_active_characters`，不再静默降级。
- 已完成：游戏初始化、新一天补位、后台补位失败时不再注入默认顾客，改为提示用户先启用角色。
- 已完成：新游戏按钮增加前置校验；无启用角色时禁用/拦截开始。

涉及文件：
- `src/utils/ai/customerGeneration.js`
- `src/hooks/useCustomerFlow.js`
- `src/hooks/useGameInit.js`
- `src/hooks/gameHandlers/useCustomerDayHandlers.js`
- `src/pages/NewGameSetupPage.jsx`
- `src/App.jsx`

## 22. 本轮新增：AI 连通性可观测性与模型可用性修正

### 22.1 已完成内容
- 已完成：连通性检查失败时弹窗展示“具体失败原因”，不再只显示泛化错误。
- 已完成：定位 302.AI 账户下 `gemini-2.5` 当前不可用（503 / 无可用模型），并切换为可用模型 `gemini-2.5-flash`。
- 已完成：实测 `chat/completions` 返回 200，构建通过。

涉及文件：
- `src/App.jsx`
- `src/config/localApiKeys.js`

## 23. 本轮新增：对话体验优化（等待态 + 去重）

### 23.1 已完成内容
- 已完成：流式回复开始前显示“思考省略号（……）”，避免空白气泡看起来像卡死。
- 已完成：收到首个流式分片后自动移除思考态；思考态消息隐藏时间戳。
- 已完成：在 RESPONSE prompt 中注入“最近两条 AI 回复禁复读约束”，并要求每轮提供新信息点。
- 已完成：OpenAI 兼容调用增加 `frequency_penalty` / `presence_penalty`，降低复读概率。

涉及文件：
- `src/hooks/useDialogue.js`
- `src/hooks/gameHandlers/useDialogueHandlers.js`
- `src/components/Chat/ChatPanel.jsx`
- `src/components/Chat/ChatPanel.css`
- `src/config/api.js`
- `src/utils/aiService.js`

## 24. 当前代码状态（截至本次更新）

- 已具备：角色 YAML 真实读取 + 角色顾客生成接入。
- 已具备：仅自定义角色测试模式（可用于严格验证角色读取是否生效）。
- 已具备：AI 连通性错误可见化与可用模型回退。
- 已具备：对话加载等待态优化与重复回复抑制。

## 25. 本轮新增：严格模式收口与“卡住可恢复”修复

### 25.1 严格仅自定义角色（收口）
- 已完成：移除“首位顾客找不到时自动降级系统顾客”的路径。
- 已完成：删除系统降级顾客模块导出与实现文件，避免后续误接回退逻辑。
- 已完成：首位顾客缺失时统一提示“请返回新游戏配置重新添加并启用角色 ID”。

涉及文件：
- `src/hooks/useGameInit.js`
- `src/utils/aiService.js`
- `src/utils/ai/fallbackCustomers.js`（已删除）

### 25.2 卡住场景可恢复（新增返回操作）
- 已完成：当加载页处于“未找到首位顾客”场景时，显示恢复按钮。
- 已完成：新增“返回新游戏配置”按钮，可直接回到角色配置页。
- 已完成：新增“返回上一级”按钮，避免只能停留在加载页。

涉及文件：
- `src/components/Game/GameLoadingScreen.jsx`
- `src/components/Game/GameLoadingScreen.css`
- `src/pages/GamePage.jsx`
- `src/App.jsx`

### 25.3 `5738g` 输入后仍找不到的根因修复
- 已完成：定位根因并修复：进游戏时槽位回灌会先清空 legacy 键，早期槽位快照里缺少角色池字段时会把刚配置的角色池一并清空。
- 已完成：在回灌前保留 `bartender_custom_character_ids` / `bartender_active_character_ids`，若当前快照未包含这两个键则恢复保留值，避免角色池被误清空。
- 已确认：`5738g` 格式校验本身合法，且远端仓库存在对应角色 YAML。

涉及文件：
- `src/utils/saveRepository.js`

### 25.4 稳定性补丁（已生效）
- 已完成：修复存档原子写入临时文件名冲突（同毫秒并发写导致 `rename ENOENT`）问题，临时文件名加入随机后缀。
- 已完成：修复氛围 API 非 JSON/空错误体时的解析异常，避免 `Unexpected end of JSON input`。

涉及文件：
- `server/save-server.mjs`
- `src/utils/ai/atmosphereGeneration.js`

## 26. 当前代码状态（最终更新）

- 已具备：严格仅自定义角色模式（无系统顾客自动补位）。
- 已具备：首位顾客缺失时可从加载页直接返回配置页或上一级，不再“卡死无出口”。
- 已具备：角色池配置在进游戏回灌阶段可保留，不再出现“输入了 5738g 但进入后丢失”的问题。
- 已具备：存档并发写入与氛围接口错误体解析的稳定性修复。

## 27. 本轮新增：角色资料目录与缓存策略重构（2026-04-16 晚）

### 27.1 已完成内容
- 已完成：角色资料目录拆分为 `seeds/characters/presets`（预置）与 `seeds/characters/added`（用户添加缓存）。
- 已完成：服务端角色读取支持本地 JSON 角色档，并保留 YAML 兼容读取路径。
- 已完成：远端角色读取成功后写入 `seeds/characters/added`，避免后续重复远端拉取。
- 已完成：预置角色 `5738g` 改为完整角色档结构，支持直接在本地调整人设参数。

涉及文件：
- `server/storyworld-service.mjs`
- `seeds/characters/README.md`
- `seeds/characters/presets/5738g.json`
- `src/config/defaultCharacters/index.js`

### 27.2 角色池体验补丁（已完成）
- 已完成：新增 `bartender_added_character_ids`，用于跨“新开档”回显已添加角色 ID。
- 已完成：添加角色前先本地判重，已存在时直接提示“该角色已添加”。
- 已完成：删除某个已添加角色后可再次添加；默认预置角色仍不可删除（直到存在非预置角色）。

涉及文件：
- `src/utils/storage.js`
- `src/pages/NewGameSetupPage.jsx`

## 28. 本轮新增：API 配置与安全流程更新（2026-04-16 晚）

### 28.1 已完成内容
- 已完成：停用 mock 模式；未配置可用 key 时返回“未配置”并提示补齐 `.env.local`。
- 已完成：`callAIAPI` 去除 mock 降级分支，调用失败不再返回模拟回复。
- 已完成：新增 `.env.example` 模板，推荐协作流程改为“模板入库、真实 key 仅本地 `.env.local`”。
- 已完成：README（中/英）同步更新 API key 协作说明。

涉及文件：
- `src/config/api.js`
- `src/utils/aiService.js`
- `src/App.jsx`
- `src/pages/SettingsPage.jsx`
- `.env.example`
- `README.md`
- `README.zh-CN.md`
- `scripts/setup-api-keys.mjs`

## 29. 当前生效状态（最新补充）

- 角色来源优先级：`seeds/characters/presets` -> `seeds/characters/added` -> 本地/远端 YAML。
- 新增角色若已存在于角色池，前端将直接提示，不触发重复读取。
- API 运行仅允许真实 provider（DeepSeek / Gemini），无 mock 回退。
- 团队协作建议：提交 `.env.example`，每位成员在本地维护 `.env.local`。

---

如需，我可以再补一份“可回滚清单”（逐文件、逐段落）用于你后续做版本提交说明或发布日志。
